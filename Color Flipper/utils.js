const btnColorElement = document.getElementById("color-button");
const bcgElement = document.getElementById("background-color");
const colorTextElement = document.getElementById("color")


function getRandomNumber(max) {
    return Math.floor(Math.random() * max);
}
